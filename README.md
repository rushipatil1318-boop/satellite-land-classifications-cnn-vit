Satellite image classification using Keras and PyTorch.

This repository contains nine executed notebooks, four trained models, the dataset archive, and evaluation results.

The saved results come from a demonstration run using 240 images per class, two training epochs, and 72 test images.
